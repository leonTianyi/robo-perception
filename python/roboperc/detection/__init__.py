"""Python detection adapters — experiment-side glue that wires a runtime backend
to the shared (native) pre/post into canonical detections. The deployment adapter
is the C++ one in detection/image/rfdetr.
"""
