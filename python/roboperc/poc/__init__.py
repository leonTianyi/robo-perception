"""Pure-Python prototype entry points for fast iteration on new models, ahead of
(or instead of) a C++ adapter. Currently: the RF-DETR PyTorch baseline that also
produces the cross-path correctness reference.
"""
