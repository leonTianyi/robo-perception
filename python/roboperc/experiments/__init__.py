"""Experiment orchestration. ``run_pipeline`` is the cross-backend comparison
(the Python replacement for the PoC's run_all.sh); it is Hydra-ready but does not
require Hydra yet — it reads a plain YAML config from configs/.
"""
